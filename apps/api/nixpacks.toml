[variables]
PORT = "3000"

[phases.setup]
# Ensure bun is available
nixPkgs = ["bun"]

[phases.install]
cmd = "bun install --frozen-lockfile"

[phases.build]
cmd = "bun run build"

[start]
cmd = "bun run start"

[healthcheck]
path = "/healthcheck"
interval = "10s"
timeout = "3s"
retries = 3
