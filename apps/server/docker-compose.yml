name: hiring3

services:
  postgres:
    image: postgres
    container_name: hiring3-postgres
    environment:
      POSTGRES_DB: hiring3
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
    ports:
      - "5432:5432"
    volumes:
      - hiring3_postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5
    restart: unless-stopped

volumes:
  hiring3_postgres_data: